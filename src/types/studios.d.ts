export type Studio = {
    id?: number;
    name?: string;
    total_seats: number;
    created_at: string;
    updated_at: string;
};

export type StudioDate = {
    id: number;
    date: string;
}

export type StudioTime = {
    id: number;
    time: string;
}